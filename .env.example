# Copy to .env.local for local development, and set the same value in your
# Vercel project settings for production. This key is only ever read by the
# serverless function in /api — it is never sent to the browser.
ANTHROPIC_API_KEY=sk-ant-...
